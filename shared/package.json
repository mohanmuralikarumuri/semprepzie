{
  "name": "@semprepzie/shared",
  "version": "1.0.0",
  "description": "Shared types and constants for Semprepzie application",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "peerDependencies": {},
  "files": [
    "dist"
  ]
}
